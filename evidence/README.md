# Task 1 Evidence

This directory contains the evidence checklist only. No live deployment or screenshots have been fabricated.

1. Live API URL: **PENDING DEPLOYMENT**
2. Terminal screenshot of `curl` against the live public URL showing pagination: **PENDING DEPLOYMENT**
3. Screenshot of the `429` response including `Retry-After`: **PENDING DEPLOYMENT**
4. Screenshot of the consumer showing data from the live API: **PENDING DEPLOYMENT**
5. Committed repeatable seed script: available in `prisma/seed.ts`

Evidence helper scripts are tools, not evidence. `scripts/live-smoke.ts` and `scripts/rate-limit-proof.ts` accept a target base URL and do not claim that deployment has occurred.
